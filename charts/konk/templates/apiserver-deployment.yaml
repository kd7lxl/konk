apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "konk.fullname" . }}-apiserver
  labels:
    {{- include "apiserver.labels" . | nindent 4 }}
spec:
{{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
{{- end }}
  selector:
    matchLabels:
      {{- include "apiserver.selectorLabels" . | nindent 6 }}
  template:
    metadata:
    {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
    {{- end }}
      labels:
        {{- include "apiserver.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "konk.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: apiserver
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - /usr/local/bin/kube-apiserver
          args:
            - --etcd-cafile=/etc/kubernetes/pki/apiserver/etcd/ca.pem
            - --etcd-certfile=/etc/kubernetes/pki/apiserver/etcd/cert.pem
            - --etcd-keyfile=/etc/kubernetes/pki/apiserver/etcd/key.pem
            - --etcd-servers=https://{{ .Release.Name }}-etcd-headless:2379 # TODO: resolve port from values
          ports:
            - name: https
              containerPort: 443
              protocol: TCP
          # livenessProbe: TODO
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
          - mountPath: /etc/kubernetes/pki/apiserver/etcd
            name: etcd-client-certs
            readOnly: true
      volumes:
      - name: etcd-client-certs
        secret:
          defaultMode: 256
          secretName: {{ include "konk.fullname" . }}-etcd-cert
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
